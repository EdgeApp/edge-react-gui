/**
 * Placeholder for CLI npm publish. The full publish pipeline is not wired in
 * this branch yet; `npm run publish:cli` builds artifacts then lands here.
 */
console.error(
  'publishCli.ts is not implemented on this branch yet.\n' +
    'Built artifacts are in lib/ after `npm run build:cli`.'
)
process.exit(1)
