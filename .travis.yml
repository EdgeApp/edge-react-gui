language: node_js
node_js:
  - 16
before_install:
  - node -r sucrase/register scripts/hack-travis.ts
install:
  - yarn install --frozen-lockfile --network-concurrency 1
  - yarn prepare
script:
  - yarn lint
  - yarn tsc
  - yarn test
