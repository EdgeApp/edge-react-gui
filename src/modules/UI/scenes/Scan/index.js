import Scan from './Scan.ui.js'
import {
  addressModalVisible,
  recipientAddress,
  scanEnabled,
  scanToWalletListModalVisibility,
  selectedWalletListModalVisibility,
  torchEnabled,
  parsedUri
} from './reducer.js'

export { Scan, addressModalVisible, recipientAddress, scanEnabled, scanToWalletListModalVisibility, selectedWalletListModalVisibility, torchEnabled, parsedUri }
export default Scan
