// @flow
import { StyleSheet } from 'react-native'

export const styles = {
  container: {
    flexDirection: 'row',
    height: 3
  },
  bar: {
    position: 'absolute',
    right: 0,
    top: -3,
    bottom: 0,
    backgroundColor: '#E9E9E9'
  }
}

export default StyleSheet.create(styles)
